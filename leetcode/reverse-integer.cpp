#include <bits/stdc++.h>

class Solution {
public:
  int reverse(int x) {
    int res;
    if (x <= 0) {
      res = -1;
    } else {
      res = 1;
    }
  }
};

int main() {
  ios_base::sync_with_stdio(false);

  return 0;
}
