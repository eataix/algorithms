#include <bits/stdc++.h>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  int T;
  cin >> T;
  for (int i = 0; i < T; ++i) {
    int A, B;
    cin >> A >> B;
    cout << A + B << endl;
  }

  return 0;
}
